// preload.js - ejecutado en el contexto del renderer con acceso limitado
// Por seguridad, no exponemos APIs de Node.js al frontend
window.addEventListener('DOMContentLoaded', () => {
  // Aquí se pueden exponer APIs seguras al frontend si se necesitan en el futuro
})
